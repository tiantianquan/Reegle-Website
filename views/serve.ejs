<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% data.forEach(function(serve){ %>
      <h2><%= serve.title %></h2>
      <p><%- serve.content %></p>
    <% }) %>
  </body>
</html>